services:
  # Fullstack Web Service (Node.js API + Static Frontend Build)
  - type: web
    name: engineering-faculty-app
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
